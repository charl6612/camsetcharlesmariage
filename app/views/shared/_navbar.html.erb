<nav class="navbar navbar-expand-lg navbar-light">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
    aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <li class="nav-item">
              <a class="btn btn_navbar" href="/#histoire">Histoire <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="btn btn_navbar" href="/#lemariage">Le mariage</a>
            </li>
            <li class="nav-item">
              <%= link_to 'Je viens !', user_invites_path(current_user, anchor: "invites"), :class => "btn btn_navbar" %>
            </li>
            <li class="nav-item">
              <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn_navbar" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "btn btn_navbar" %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>
