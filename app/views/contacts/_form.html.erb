 <div class="container">
        <div class="row justify-content-md-center">
          <div class="col-12 col-sm-6 col-md-8 text-center">
<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>          <div class="form-group">
  <% end %>

          <div class="form-group">

        <div class="form-inputs">
    <%= f.label :Prénom %><br />
    <%= f.text_field :first_name, class:"form-control", id: "prenom", placeholder: "Prénom", required: true  %>
  </div>
  </div>


        <div class="form-inputs">
    <%= f.label :message %><br />
    <%= f.text_area :message, class:"form-control", id:"email", placeholder: "Message", required: true %>
  </div>
  <div class="form-inputs">
    <%= f.submit "Envoyer",class: "btn btn-outline-primary"%>
  </div>
<% end %>
              </div>
            </div>
                </div>